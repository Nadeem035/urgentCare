1. ALTER TABLE `service` ADD `meta_title` VARCHAR(255) NOT NULL AFTER `has_children`, ADD `meta_key` VARCHAR(255) NOT NULL AFTER `meta_title`, ADD `meta_desc` VARCHAR(255) NOT NULL AFTER `meta_key`, ADD INDEX (`meta_title`), ADD INDEX (`meta_key`), ADD INDEX (`meta_desc`);
